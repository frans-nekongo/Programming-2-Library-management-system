public class Order{
    Book booc = new Book();
    Employee emp=new Employee();
    //Newspaper newP = new Newspaper();

    String npName,bName,bType,npPublisher;
    int npCount,bCountiD,countOfBooks;

    //constructors
    Order(){
        //from book//
        booc.setName(bName);
        booc.setType(bType);
        booc.setCountID(bCountiD);
        booc.setCountOfBooks(countOfBooks);
        //from newspaper//

    };
}
